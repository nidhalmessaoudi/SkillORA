<<<<<<< Updated upstream
{% extends 'layouts/admin.html.twig' %}

{% block title %}Evaluations - Admin - SkillHarbor{% endblock %}

{% block admin_content %}
<div class="mb-8 flex items-center justify-between">
    <div>
        <h1 class="text-display-lg font-display font-bold text-navy-900 dark:text-white mb-2">
            Evaluations
        </h1>
        <p class="text-body-md text-navy-600 dark:text-navy-400">
            Manage all platform evaluations (quizzes & exams).
        </p>
    </div>

    <a href="{{ path('evaluation_new') }}" class="btn-primary btn-sm">
        <i data-lucide="plus" class="w-4 h-4"></i>
        New Evaluation
    </a>
</div>

<div class="card">
    <div class="p-6 border-b border-navy-100 dark:border-navy-800">
        <h2 class="text-heading-lg font-display font-semibold text-navy-900 dark:text-white">
            All Evaluations ({{ evaluations|length }})
        </h2>
    </div>

    <div class="overflow-x-auto">
        <table class="w-full">
            <thead class="bg-navy-50 dark:bg-navy-900/50">
                <tr>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        ID
                    </th>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Title
                    </th>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Type
                    </th>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Duration
                    </th>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Total Score
                    </th>
                    <th class="px-6 py-3 text-left text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Created At
                    </th>
                    <th class="px-6 py-3 text-right text-caption font-semibold text-navy-600 dark:text-navy-400 uppercase tracking-wider">
                        Actions
                    </th>
                </tr>
            </thead>

            <tbody class="divide-y divide-navy-100 dark:divide-navy-800">
                {% for evaluation in evaluations %}
                <tr class="hover:bg-navy-50 dark:hover:bg-navy-900/30 transition-colors">
                    <td class="px-6 py-4 whitespace-nowrap text-body-sm text-navy-600 dark:text-navy-300">
                        #{{ evaluation.id }}
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex flex-col">
                            <span class="text-body-sm font-medium text-navy-900 dark:text-white">
                                {{ evaluation.title }}
                            </span>
                            {% if evaluation.description %}
                                <span class="text-caption text-navy-500 truncate max-w-xs">
                                    {{ evaluation.description }}
                                </span>
                            {% endif %}
                        </div>
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap text-body-sm">
                        <span class="badge 
                            {{ evaluation.type == 'EXAM' 
                                ? 'bg-danger-100 text-danger-700' 
                                : 'bg-harbor-100 text-harbor-700' }}">
                            {{ evaluation.type }}
                        </span>
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap text-body-sm text-navy-600 dark:text-navy-300">
                        {{ evaluation.duration }} min
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap text-body-sm text-navy-600 dark:text-navy-300">
                        {{ evaluation.totalScore }}
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap text-body-sm text-navy-600 dark:text-navy-300">
                        {{ evaluation.createdAt|date('M d, Y H:i') }}
                    </td>

                    <td class="px-6 py-4 whitespace-nowrap text-right">
                        <div class="flex items-center justify-end gap-2">
                            <a href="{{ path('evaluation_show', {id: evaluation.id}) }}"
                               class="btn-sm bg-navy-100 text-navy-700 hover:bg-navy-200">
                                View
                            </a>

                            <a href="{{ path('evaluation_edit', {id: evaluation.id}) }}"
                               class="btn-sm bg-harbor-100 text-harbor-700 hover:bg-harbor-200">
                                Edit
                            </a>

                            <form method="post"
                                  action="{{ path('evaluation_delete', {id: evaluation.id}) }}"
                                  onsubmit="return confirm('Delete this evaluation?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation.id) }}">
                                <button type="submit"
                                        class="btn-sm bg-danger-100 text-danger-600 hover:bg-danger-200">
                                    Delete
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="7" class="px-6 py-12 text-center">
                        <div class="empty-state">
                            <div class="empty-state-icon">
                                <i data-lucide="clipboard-list" class="w-full h-full"></i>
                            </div>
                            <p class="text-navy-500">No evaluations found</p>
                        </div>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
=======
{# RIGHT: QUESTIONS PANEL #}
<div class="lg:col-span-1">
  <div class="card shadow-sm rounded-xl overflow-hidden">

    {# HEADER #}
    <div class="p-6 border-b">
      <div class="flex items-center justify-between gap-3">
        <h3 class="text-lg font-semibold">
          {{ selectedEvaluation ? (selectedEvaluation.type == 'EXAM' ? 'Exam' : 'Questions') : 'Questions' }}
        </h3>

        {% if selectedEvaluation %}
          {% if selectedEvaluation.type == 'QUIZ' %}
            <div class="flex items-center gap-2">

              {# ✅ Edit Questions (go to create question, then back) #}
              <a href="{{ path('admin_question_new', {
                evaluationId: selectedEvaluation.id,
                back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
              }) }}"
                 class="btn-secondary btn-sm">
                Edit Questions
              </a>

              {# ✅ View Answers (go to answers page filtered by evaluation) #}
              <a href="{{ path('admin_answer_index', {
                evaluationId: selectedEvaluation.id,
                back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
              }) }}"
                 class="btn-primary btn-sm">
                View Answers
              </a>

            </div>
          {% else %}
            <a href="{{ path('admin_exam_manage', { id: selectedEvaluation.id }) }}"
               class="btn-primary btn-sm">
              Manage Exam
            </a>
          {% endif %}
        {% endif %}
      </div>
    </div>

    {# BODY #}
    <div class="p-6 space-y-4">
      {% if not selectedEvaluation %}
        <div class="text-gray-500 text-sm">
          Click on an evaluation title to view its questions.
        </div>
      {% else %}
        <div>
          <div class="font-semibold text-gray-900">
            {{ selectedEvaluation.title }}
          </div>
          <div class="text-xs text-gray-500 mt-1">
            {{ questions|length }} question(s)
          </div>
        </div>

        {% if selectedEvaluation.type == 'QUIZ' %}
          <div class="space-y-3">
            {% for q in questions %}
              <div class="p-3 rounded-lg border bg-white">
                <div class="text-sm font-medium whitespace-pre-wrap">{{ q.content }}</div>

                <div class="text-xs text-gray-500 mt-1">
                  Type: {{ q.type }} • Score: {{ q.score }}
                </div>

                {# ACTIONS per question #}
                <div class="mt-3 flex justify-end gap-2">

                  {# ✅ View Answers for MCQ #}
                  {% if q.type == 'MCQ' %}
                    <a class="btn-sm"
                       href="{{ path('admin_question_show', {
                         id: q.id,
                         back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
                       }) }}">
                      View Answers
                    </a>
                  {% endif %}

                  <a class="btn-sm"
                     href="{{ path('admin_question_edit', {
                       id: q.id,
                       evaluationId: selectedEvaluation.id,
                       back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
                     }) }}">
                    Edit
                  </a>

                  <form method="post"
                        action="{{ path('admin_question_delete', {
                          id: q.id,
                          back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
                        }) }}"
                        onsubmit="return confirm('Delete this question?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete_question_' ~ q.id) }}">
                    <button type="submit" class="btn-sm text-red-600 hover:text-red-800">
                      Delete
                    </button>
                  </form>

                </div>
              </div>
            {% else %}
              <div class="text-gray-500 text-sm">No questions for this evaluation.</div>
            {% endfor %}
          </div>

          {# FOOTER: Add Question #}
          <div class="pt-3 border-t">
            <a href="{{ path('admin_question_new', {
              evaluationId: selectedEvaluation.id,
              back: path('evaluation_index', { type: selectedType, evaluationId: selectedEvaluation.id })
            }) }}"
               class="btn-primary btn-sm w-full text-center block">
              + Add Question
            </a>
          </div>

        {% else %}
          <div class="text-gray-500 text-sm">
            This is an exam. Click <b>Manage Exam</b> to manage its questions.
          </div>
        {% endif %}
      {% endif %}
    </div>

  </div>
</div>
>>>>>>> Stashed changes
