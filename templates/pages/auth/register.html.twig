{% extends 'layouts/base.html.twig' %}

{% block title %}Create Account - SkillHarbor{% endblock %}

{% block header %}{% endblock %}
{% block footer %}{% endblock %}

{% block body %}
<div class="min-h-screen flex bg-white dark:bg-navy-950">
    {# Left Panel - Form #}
    <div class="w-full lg:w-1/2 flex flex-col">
        {# Header #}
        <div class="flex items-center justify-between p-6">
            <a href="{{ path('app_home') }}" class="inline-flex items-center gap-2.5 group">
                <div class="w-9 h-9 bg-gradient-to-br from-harbor-600 to-harbor-500 rounded-xl flex items-center justify-center">
                    <i data-lucide="anchor" class="w-4.5 h-4.5 text-white"></i>
                </div>
                <span class="font-display font-bold text-lg text-navy-900 dark:text-white">SkillHarbor</span>
            </a>
            <button onclick="toggleTheme()" class="btn-ghost btn-icon">
                <i data-lucide="sun" class="w-5 h-5 dark:hidden"></i>
                <i data-lucide="moon" class="w-5 h-5 hidden dark:block"></i>
            </button>
        </div>

        {# Form Container #}
        <div class="flex-1 flex items-center justify-center px-6 lg:px-16 py-8 overflow-y-auto">
            <div class="w-full max-w-md">
                {# Header #}
                <div class="mb-8">
                    <h1 class="font-display font-bold text-display-sm text-navy-900 dark:text-white mb-2">
                        Create your account
                    </h1>
                    <p class="text-body-md text-navy-500 dark:text-navy-400">
                        Join 500,000+ learners today
                    </p>
                </div>

                {# Role Selection #}
                <div class="mb-6">
                    <label class="input-label">I want to</label>
                    <div class="grid grid-cols-2 gap-3">
                        <label class="cursor-pointer">
                            <input type="radio" name="role_selector" value="student" class="peer hidden" checked onchange="document.getElementById('role-input').value = 'student'">
                            <div class="p-4 rounded-2xl border-2 border-navy-200 dark:border-navy-700 bg-white dark:bg-navy-900 peer-checked:border-harbor-500 peer-checked:bg-harbor-50 dark:peer-checked:bg-harbor-900/30 transition-all text-center">
                                <i data-lucide="graduation-cap" class="w-6 h-6 mx-auto mb-2 text-harbor-600 dark:text-harbor-400"></i>
                                <div class="font-semibold text-navy-900 dark:text-white text-body-sm">Learn</div>
                            </div>
                        </label>
                        <label class="cursor-pointer">
                            <input type="radio" name="role_selector" value="professor" class="peer hidden" onchange="document.getElementById('role-input').value = 'professor'">
                            <div class="p-4 rounded-2xl border-2 border-navy-200 dark:border-navy-700 bg-white dark:bg-navy-900 peer-checked:border-harbor-500 peer-checked:bg-harbor-50 dark:peer-checked:bg-harbor-900/30 transition-all text-center">
                                <i data-lucide="presentation" class="w-6 h-6 mx-auto mb-2 text-coral-500 dark:text-coral-400"></i>
                                <div class="font-semibold text-navy-900 dark:text-white text-body-sm">Teach</div>
                            </div>
                        </label>
                    </div>
                </div>

                {# Google Button #}
                <a href="{{ path('connect_google_start') }}" class="btn-outline w-full mb-6 flex items-center justify-center gap-3">
                    <svg class="w-5 h-5" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                    </svg>
                    Continue with Google
                </a>

                {# Divider #}
                <div class="flex items-center gap-4 mb-6">
                    <div class="flex-1 h-px bg-navy-200 dark:bg-navy-800"></div>
                    <span class="text-caption text-navy-400 uppercase">or</span>
                    <div class="flex-1 h-px bg-navy-200 dark:bg-navy-800"></div>
                </div>

                {# Error Message #}
                {% if error %}
                    <div class="mb-6 p-4 rounded-2xl bg-danger-100 dark:bg-danger-900/30 border border-danger-200 dark:border-danger-800">
                        <div class="flex items-center gap-3">
                            <i data-lucide="alert-circle" class="w-5 h-5 text-danger-600 dark:text-danger-400"></i>
                            <p class="text-body-sm text-danger-700 dark:text-danger-400">{{ error }}</p>
                        </div>
                    </div>
                {% endif %}

                {# Form #}
                <form method="post" action="{{ path('auth_register') }}" id="register-form">
                    <div class="space-y-4">
                        {# Name Fields #}
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label for="firstname" class="input-label">First name</label>
                                <input type="text" id="firstname" name="firstname" placeholder="John" autocomplete="given-name" class="input" required>
                            </div>
                            <div>
                                <label for="lastname" class="input-label">Last name</label>
                                <input type="text" id="lastname" name="lastname" placeholder="Doe" autocomplete="family-name" class="input" required>
                            </div>
                        </div>

                        {# Email #}
                        <div>
                            <label for="email" class="input-label">Email</label>
                            <input type="email" id="email" name="email" placeholder="you@example.com" autocomplete="email" class="input" required>
                        </div>

                        {# Hidden Role Field #}
                        <input type="hidden" name="role" id="role-input" value="student">

                        {# Password #}
                        <div>
                            <label for="password" class="input-label">Password</label>
                            <div class="relative">
                                <input type="password" id="password" name="password" placeholder="Min. 8 characters" autocomplete="new-password" class="input pr-12" required>
                                <button type="button" onclick="togglePassword('password', 'pwd-icon')" class="absolute right-3 top-1/2 -translate-y-1/2 text-navy-400 hover:text-navy-600 dark:hover:text-navy-300">
                                    <i data-lucide="eye" class="w-5 h-5" id="pwd-icon"></i>
                                </button>
                            </div>
                            {# Strength Indicator #}
                            <div class="flex items-center gap-3 mt-2">
                                <div class="flex gap-1 flex-1">
                                    <div id="str-1" class="h-1 flex-1 rounded-full bg-navy-200 dark:bg-navy-700"></div>
                                    <div id="str-2" class="h-1 flex-1 rounded-full bg-navy-200 dark:bg-navy-700"></div>
                                    <div id="str-3" class="h-1 flex-1 rounded-full bg-navy-200 dark:bg-navy-700"></div>
                                </div>
                                <span id="str-text" class="text-caption font-medium text-navy-400"></span>
                            </div>
                        </div>

                        {# Confirm Password #}
                        <div>
                            <label for="confirm-password" class="input-label">Confirm password</label>
                            <div class="relative">
                                <input type="password" id="confirm-password" name="confirm-password" placeholder="Confirm your password" autocomplete="new-password" class="input pr-12" required>
                                <button type="button" onclick="togglePassword('confirm-password', 'confirm-icon')" class="absolute right-3 top-1/2 -translate-y-1/2 text-navy-400 hover:text-navy-600 dark:hover:text-navy-300">
                                    <i data-lucide="eye" class="w-5 h-5" id="confirm-icon"></i>
                                </button>
                            </div>
                        </div>

                        {# Terms #}
                        <div class="flex items-start gap-3">
                            <input type="checkbox" id="terms" name="terms" class="checkbox mt-0.5" required>
                            <label for="terms" class="text-body-sm text-navy-600 dark:text-navy-400 cursor-pointer">
                                I agree to the <a href="#" class="text-harbor-600 dark:text-harbor-400 hover:underline">Terms</a> and <a href="#" class="text-harbor-600 dark:text-harbor-400 hover:underline">Privacy Policy</a>
                            </label>
                        </div>

                        {# Submit #}
                        <button type="submit" class="btn-primary w-full">
                            Create account
                        </button>
                    </div>
                </form>

                {# Login Link #}
                <p class="mt-8 text-center text-body-sm text-navy-500 dark:text-navy-400">
                    Already have an account?
                    <a href="{{ path('auth_login') }}" class="font-semibold text-harbor-600 dark:text-harbor-400 hover:underline ml-1">
                        Sign in
                    </a>
                </p>
            </div>
        </div>

        {# Footer #}
        <div class="p-6 text-center">
            <p class="text-caption text-navy-400 flex items-center justify-center gap-2">
                <i data-lucide="shield-check" class="w-4 h-4"></i>
                Your data is protected with 256-bit encryption
            </p>
        </div>
    </div>

    {# Right Panel - Brand #}
    <div class="hidden lg:block lg:w-1/2 bg-gradient-to-br from-harbor-600 to-harbor-500 relative">
        {# Subtle background #}
        <div class="absolute inset-0">
            <div class="absolute top-20 right-20 w-64 h-64 bg-white/10 rounded-full blur-3xl"></div>
            <div class="absolute bottom-20 left-20 w-80 h-80 bg-harbor-400/20 rounded-full blur-3xl"></div>
        </div>

        {# Content #}
        <div class="relative h-full flex flex-col justify-center p-12 lg:p-16">
            <div class="max-w-md">
                <h2 class="font-display font-bold text-3xl lg:text-4xl text-white mb-4">
                    Start your learning journey
                </h2>
                <p class="text-lg text-white/80 mb-10">
                    Access 10,000+ courses from industry experts and earn certificates that matter.
                </p>

                {# Features #}
                <div class="space-y-4 mb-10">
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-2xl bg-white/20 flex items-center justify-center">
                            <i data-lucide="play-circle" class="w-5 h-5 text-white"></i>
                        </div>
                        <span class="text-white/90">Unlimited access to all courses</span>
                    </div>
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-2xl bg-white/20 flex items-center justify-center">
                            <i data-lucide="award" class="w-5 h-5 text-white"></i>
                        </div>
                        <span class="text-white/90">Earn verified certificates</span>
                    </div>
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-2xl bg-white/20 flex items-center justify-center">
                            <i data-lucide="users" class="w-5 h-5 text-white"></i>
                        </div>
                        <span class="text-white/90">Join a community of 500K+ learners</span>
                    </div>
                </div>

                {# Testimonial #}
                <div class="bg-white/10 backdrop-blur rounded-3xl p-6">
                    <div class="flex items-center gap-1 mb-3">
                        {% for i in 1..5 %}
                        <i data-lucide="star" class="w-4 h-4 text-warning-400 fill-warning-400"></i>
                        {% endfor %}
                    </div>
                    <p class="text-white/90 mb-4">
                        "I landed my dream job at Netflix after completing courses here. Best decision ever!"
                    </p>
                    <div class="flex items-center gap-3">
                        <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=80&h=80&fit=crop" alt="" class="w-10 h-10 rounded-full object-cover">
                        <div>
                            <div class="font-semibold text-white text-body-sm">Marcus Johnson</div>
                            <div class="text-white/60 text-caption">Software Engineer at Netflix</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
    lucide.createIcons();

    function togglePassword(inputId, iconId) {
        const input = document.getElementById(inputId);
        const icon = document.getElementById(iconId);
        if (input.type === 'password') {
            input.type = 'text';
            icon.setAttribute('data-lucide', 'eye-off');
        } else {
            input.type = 'password';
            icon.setAttribute('data-lucide', 'eye');
        }
        lucide.createIcons();
    }

    // Password strength
    const pwd = document.getElementById('password');
    const s1 = document.getElementById('str-1');
    const s2 = document.getElementById('str-2');
    const s3 = document.getElementById('str-3');
    const sText = document.getElementById('str-text');

    pwd.addEventListener('input', function() {
        const p = this.value;
        let score = 0;
        if (p.length >= 8) score++;
        if (/[0-9!@#$%^&*]/.test(p)) score++;
        if (/[A-Z]/.test(p)) score++;

        [s1, s2, s3].forEach(b => b.className = 'h-1 flex-1 rounded-full bg-navy-200 dark:bg-navy-700');

        if (p.length === 0) {
            sText.textContent = '';
        } else if (score <= 1) {
            s1.classList.remove('bg-navy-200', 'dark:bg-navy-700');
            s1.classList.add('bg-danger-500');
            sText.textContent = 'Weak';
            sText.className = 'text-caption font-medium text-danger-500';
        } else if (score === 2) {
            [s1, s2].forEach(b => {
                b.classList.remove('bg-navy-200', 'dark:bg-navy-700');
                b.classList.add('bg-warning-500');
            });
            sText.textContent = 'Good';
            sText.className = 'text-caption font-medium text-warning-500';
        } else {
            [s1, s2, s3].forEach(b => {
                b.classList.remove('bg-navy-200', 'dark:bg-navy-700');
                b.classList.add('bg-success-500');
            });
            sText.textContent = 'Strong';
            sText.className = 'text-caption font-medium text-success-500';
        }
    });
</script>
{% endblock %}
