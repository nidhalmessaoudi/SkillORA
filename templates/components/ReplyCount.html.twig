<div {{ attributes }} class="flex items-center gap-1 text-slate-500 dark:text-slate-400 hover:text-emerald-600 dark:hover:text-emerald-400 transition-colors">
    <i data-lucide="message-square" class="w-4 h-4"></i>
    <span>{{ count }} {{ count == 1 ? 'reply' : 'replies' }}</span>
</div>
